diff --git a/src/app.py b/src/app.py
index 0365b1f..3b768b4 100644
--- a/src/app.py
+++ b/src/app.py
@@ -14,6 +14,7 @@ from typing import Optional
 import cv2
 import numpy as np
 import torch
+import trimesh
 import tyro
 import viser
 from rich.console import Console
@@ -53,6 +54,8 @@ def app(
     max_depth: float = 5.0,
     share: bool = False,
     save_pred: bool = False,
+    overlay_mesh: Optional[Path] = None,
+    overlay_mesh_opacity: float = 0.28,
 ) -> None:
     """Launch an interactive Viser app: click a pixel to predict a grasp there.

@@ -68,6 +71,8 @@ def app(
         share: Generate a shareable Viser URL.
         save_pred: Save each click to grasp_pred/<name>_<datetime_ms>.pkl (a new
             file per click) for later viewing with visualize_predictions.
+        overlay_mesh: Optional object mesh already expressed in the selected RGB-D camera frame.
+        overlay_mesh_opacity: Opacity of the optional object mesh overlay.
     """
     checkpoint_path = resolve_checkpoint_path(checkpoint_path)
     device = "cuda" if torch.cuda.is_available() else "cpu"
@@ -75,6 +80,21 @@ def app(
     use_rgb = getattr(model, "use_rgb", True)
     use_depth = getattr(model, "use_depth", False)
     pcl_use_rgb = getattr(model, "pcl_use_rgb", False)
+    if not 0.0 <= overlay_mesh_opacity <= 1.0:
+        raise ValueError("overlay_mesh_opacity must be in [0, 1]")
+
+    overlay_mesh_geometry = None
+    if overlay_mesh is not None:
+        raw_overlay = trimesh.load_mesh(overlay_mesh, process=False)
+        if isinstance(raw_overlay, trimesh.Scene):
+            raw_overlay = trimesh.util.concatenate(tuple(raw_overlay.geometry.values()))
+        if not isinstance(raw_overlay, trimesh.Trimesh) or len(raw_overlay.faces) == 0:
+            raise ValueError(f"No triangle mesh found: {overlay_mesh}")
+        overlay_vertices = np.asarray(raw_overlay.vertices, dtype=np.float32)
+        overlay_faces = np.asarray(raw_overlay.faces, dtype=np.int32)
+        if not np.isfinite(overlay_vertices).all() or np.any(overlay_vertices[:, 2] <= 0):
+            raise ValueError("overlay_mesh must be finite and in front of the RGB-D camera")
+        overlay_mesh_geometry = (overlay_vertices, overlay_faces)

     # Resolve sample names. Pkls are found recursively under dataset_path
     # (grasp_pred/ excluded), so flat and nested scene/object layouts both work;
@@ -163,6 +183,9 @@ def app(
             step=0.1,
             initial_value=min(max_depth, 20.0),
         )
+        gui_show_overlay_mesh = server.gui.add_checkbox(
+            "Aligned Object Mesh", overlay_mesh_geometry is not None
+        )

     vis = {
         "rgb_image": None,
@@ -173,6 +196,7 @@ def app(
         "depth_point_cloud": None,
         "camera_frustum": None,
         "camera_frame": None,
+        "overlay_mesh": None,
         "last_click_norm": None,
     }

@@ -458,6 +482,19 @@ def app(
                 state["scene_handles"].append(dpc)
                 vis["depth_point_cloud"] = dpc

+        if overlay_mesh_geometry is not None:
+            overlay_vertices, overlay_faces = overlay_mesh_geometry
+            overlay_handle = server.scene.add_mesh_simple(
+                "/aligned_object_mesh",
+                vertices=overlay_vertices,
+                faces=overlay_faces,
+                color=(73, 196, 186),
+            )
+            overlay_handle.opacity = overlay_mesh_opacity
+            overlay_handle.visible = gui_show_overlay_mesh.value
+            state["scene_handles"].append(overlay_handle)
+            vis["overlay_mesh"] = overlay_handle
+
         # Camera frustum and frame
         fov = 2 * np.arctan2(height / 2, K[1, 1])
         aspect = width / height
@@ -510,6 +547,10 @@ def app(
             vis["depth_point_cloud"].point_size = gui_depth_point_size.value
             vis["depth_point_cloud"].visible = gui_depth_point_size.value > 0

+    def on_show_overlay_mesh(_):
+        if vis.get("overlay_mesh"):
+            vis["overlay_mesh"].visible = gui_show_overlay_mesh.value
+
     def on_show_grasp(_):
         pred_store.set_visible(
             gui_show_grasp.value,
@@ -549,6 +590,7 @@ def app(
     gui_mesh_opacity.on_update(on_mesh_opacity)
     gui_depth_point_size.on_update(on_depth_point_size)
     gui_show_grasp.on_update(on_show_grasp)
+    gui_show_overlay_mesh.on_update(on_show_overlay_mesh)
     gui_show_point_condition.on_update(on_show_point_condition)
     gui_show_wrist_frame.on_update(on_show_wrist)
     gui_show_rgb.on_update(on_show_rgb)
diff --git a/src/inference.py b/src/inference.py
index 74c8b90..4e65b1f 100644
--- a/src/inference.py
+++ b/src/inference.py
@@ -217,6 +217,7 @@ def main(
         stems = batch["stem"]
         rgb = batch["rgb"].to(device) if use_rgb else None
         pcl_xyz = batch["pcl_xyz"].to(device) if use_depth else None
+        pcl_rgb = batch["pcl_rgb"].to(device) if use_depth else None

         if device == "cuda":
             torch.cuda.synchronize()
@@ -228,6 +229,7 @@ def main(
                 steps=sampling_steps,
                 rgb=rgb,
                 pcl_xyz=pcl_xyz,
+                pcl_rgb=pcl_rgb,
             )
         if device == "cuda":
             torch.cuda.synchronize()
diff --git a/src/utils/pcl_utils.py b/src/utils/pcl_utils.py
index 6b820b6..0e6edd2 100644
--- a/src/utils/pcl_utils.py
+++ b/src/utils/pcl_utils.py
@@ -100,6 +100,7 @@ def depth_to_pcl_tensors(
     max_depth: float = 3.0,
     center: Optional[np.ndarray] = None,
     crop_radius: Optional[float] = None,
+    rng: Optional[np.random.Generator] = None,
 ) -> tuple[torch.Tensor, torch.Tensor]:
     """End-to-end: depth + rgb + K → fixed-size (xyz, rgb) tensors.

@@ -126,7 +127,7 @@ def depth_to_pcl_tensors(
         center=center,
         crop_radius=crop_radius,
     )
-    xyz_np, rgb_np = sample_fixed_n(xyz_np, rgb_np, n_points)
+    xyz_np, rgb_np = sample_fixed_n(xyz_np, rgb_np, n_points, rng=rng)
     xyz = torch.from_numpy(xyz_np).float()
     rgb_pcl = torch.from_numpy(rgb_np).float() / 255.0
     return xyz, rgb_pcl
diff --git a/src/visualize_predictions.py b/src/visualize_predictions.py
index 8679792..59ec566 100644
--- a/src/visualize_predictions.py
+++ b/src/visualize_predictions.py
@@ -48,6 +48,7 @@ def visualize(
     sample_name: Optional[str] = None,
     num_samples: int = 100,
     share: bool = False,
+    object_pointcloud: Optional[Path] = None,
 ) -> None:
     """Visualize grasp predictions.

@@ -58,6 +59,7 @@ def visualize(
         sample_name: Sample to visualize or path to .txt file with one stem per line.
         num_samples: Number of random samples to load. Ignored if sample_name is set.
         share: Generate shareable Viser URL.
+        object_pointcloud: Optional selected-object NPZ with points_camera, shown as a yellow overlay.
     """
     server = viser.ViserServer(port=port, verbose=False)
     server.gui.configure_theme(dark_mode=True)
@@ -87,6 +89,19 @@ def visualize(
         else:
             sample_names = all_samples

+    selected_object_points = None
+    if object_pointcloud is not None:
+        if not object_pointcloud.is_file():
+            raise FileNotFoundError(object_pointcloud)
+        with np.load(object_pointcloud, allow_pickle=False) as pointcloud_data:
+            selected_object_points = np.asarray(
+                pointcloud_data["points_camera"], dtype=np.float32
+            )
+        if selected_object_points.ndim != 2 or selected_object_points.shape[1] != 3:
+            raise ValueError("object_pointcloud points_camera must have shape (N, 3)")
+        if not np.isfinite(selected_object_points).all():
+            raise ValueError("object_pointcloud contains NaN or Inf")
+
     state = {"current_idx": 0, "scene_handles": []}
     server.scene.set_up_direction("-y")

@@ -128,6 +143,12 @@ def visualize(
             initial_value=min(max_depth, 20.0),
         )
         gui_crop_only = server.gui.add_checkbox("Crop PCL", False)
+        gui_show_selected_object = server.gui.add_checkbox(
+            "Selected Object PCL", selected_object_points is not None
+        )
+        gui_selected_object_point_size = server.gui.add_slider(
+            "Selected Object Point Size", min=0.0, max=0.01, step=0.0001, initial_value=0.003
+        )

     vis = {
         "frustum": None,
@@ -147,8 +168,22 @@ def visualize(
         "fov": None,
         "aspect": None,
         "pred_handles": [],
+        "selected_object_point_cloud": None,
     }

+    if selected_object_points is not None:
+        selected_colors = np.tile(
+            np.asarray([[255, 190, 0]], dtype=np.uint8),
+            (selected_object_points.shape[0], 1),
+        )
+        vis["selected_object_point_cloud"] = server.scene.add_point_cloud(
+            "/selected_object_points",
+            points=selected_object_points,
+            colors=selected_colors,
+            point_size=gui_selected_object_point_size.value,
+            point_shape="rounded",
+        )
+
     def update_sidebar_image():
         img = vis["rgb_image"]
         if img is None:
@@ -489,6 +524,14 @@ def visualize(
             )
         apply_background_dim()

+    def on_selected_object_visibility(_):
+        if vis["selected_object_point_cloud"]:
+            vis["selected_object_point_cloud"].visible = gui_show_selected_object.value
+
+    def on_selected_object_point_size(_):
+        if vis["selected_object_point_cloud"]:
+            vis["selected_object_point_cloud"].point_size = gui_selected_object_point_size.value
+
     def on_crop(_):
         rebuild_crop()

@@ -522,6 +565,8 @@ def visualize(
     gui_show_camera.on_update(on_show_rgb)
     gui_mesh_opacity.on_update(on_mesh_opacity)
     gui_depth_point_size.on_update(on_depth_point_size)
+    gui_show_selected_object.on_update(on_selected_object_visibility)
+    gui_selected_object_point_size.on_update(on_selected_object_point_size)
     gui_show_camera_frame.on_update(on_show_camera_frame)
     gui_show_wrist_frame.on_update(on_show_wrist)
     gui_show_grasp.on_update(on_show_grasp)
